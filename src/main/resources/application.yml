spring:
  application:
    name: solar-sync-broker-api
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3313/solarsyncbroker_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

springdoc:
  swagger-ui:
    path: /swagger-ui.html

application:
  jwt:
    secretKey: ${JWT_SECRET_KEY:dev-only-solarsync-secret-key-please-change-in-prod-0123456789ABCDEF}
    tokenExpirationAfterDays: 7

solarsync:
  simulation:
    seed: 42
    tick-seconds: 60
    real-to-virtual-ratio: 60
  grid:
    transformer-nominal-kw: 50
    safety-margin-percent: 10
  agent:
    mode: ADVISORY
    planning-interval-seconds: 300
